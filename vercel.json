{
  "version": 2,
  "buildCommand": "ng build --configuration production --output-path=dist",
  "outputDirectory": "dist/browser",
  "installCommand": "npm install",
  "framework": "angular",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
